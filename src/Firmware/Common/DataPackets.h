{{ ... }}
const IPAddress TOUGHBOOK_IP(10, 0, 0, 100);

// UDP Ports
const uint16_t SENSOR_DATA_PORT = 2390;
const uint16_t HYDRAULIC_COMMAND_PORT = 2391;
const uint16_t RTCM_CORRECTION_PORT = 2392; // Port for broadcasting RTK corrections

// Data Packet Structures

// Sent from Outer Teensys to Toughbook
{{ ... }}
